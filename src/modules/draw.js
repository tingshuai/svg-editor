// import shop from '../../api/shop'

// initial state
const state = {

}

// getters
const getters = {}

// actions
const actions = {
  getAllProducts ({ commit }) {
    
  }
}

// mutations
const mutations = {
  getPage(state, getters, rootState, rootGetters){
    debugger;
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}