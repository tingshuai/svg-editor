// import shop from '../../api/shop'

// initial state
const state = {

}

// getters
const getters = {}

// actions
const actions = {
  getAllProducts ({ commit }) {
    
  }
}

// mutations
const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}